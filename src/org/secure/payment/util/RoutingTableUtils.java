package org.secure.payment.util;

import java.util.HashMap;

public class RoutingTableUtils {
	
	/*public void populateStaticRoutingTable (Map<Integer, Routing> routingTable) {
		// For Router1
		Routing link = new Routing(ROUTER_ID, LINK_COST, 10001);
		routingTable.put(10001, link);
		
		link = new Routing(ROUTER_ID, LINK_COST, 10002);
		routingTable.put(10002, link);
		
		link = new Routing(ROUTER_ID, LINK_COST, 90003);
		routingTable.put(90003, link);
	}*/
}
